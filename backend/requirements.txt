# ============================================
# ARCHIVO 3/10: DEPENDENCIAS DE PYTHON
# ============================================
# Este archivo lista todos los paquetes que necesita el proyecto
# Para instalarlos: pip install -r requirements.txt
# ============================================

Flask==2.3.3           # Framework web para Python
flask-cors==4.0.0      # Permite conexiones desde el frontend
mysql-connector-python==8.1.0  # Conector para MySQL (ya lo tienes)
python-dotenv==1.0.0   # Para variables de entorno (lo usaremos después)